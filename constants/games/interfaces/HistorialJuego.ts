interface HistorialJuego {
    id?: string; // Nuevo campo para el ID del documento
    id_users: string; // El ID del usuario
    id_Game: string; // El ID del juego
    results: any; // Puedes definir un tipo más específico según la estructura de los resultados
  }